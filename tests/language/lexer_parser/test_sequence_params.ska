main {
    createGroup paramGroup(rectangle1);
    parameterSequence(paramGroup, 200, 200, 0, 500);
}

sequence parameterSequence(group, x, y, start, duration) {
    moveTo(group, start, duration, x, y);
}
